<!-- Dashboard específico para PTD (id_role = 3) -->
<div class="ptd-dashboard">
    
    <!-- Seção de Trilhas -->
    <div class="dashboard-section trails-section">
        <div class="section-header">
            <h2 class="section-title">Suas Trilhas</h2>
            <button id="refresh-trails" class="refresh-btn" title="Atualizar trilhas">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
                </svg>
            </button>
        </div>
        
        <div class="trails-container">
            <!-- TF: Trilhas sendo feitas -->
            <div class="trail-group">
                <h3 class="trail-group-title">
                    <span class="trail-icon">📚</span>
                    Trilhas em Andamento
                </h3>
                <div id="active-trails" class="trail-list">
                    <!-- Trilhas serão carregadas dinamicamente -->
                    <div class="trail-loading">Carregando trilhas...</div>
                </div>
            </div>
            
            <!-- TA: Trilhas atribuídas pelo Gestor -->
            <div class="trail-group">
                <h3 class="trail-group-title">
                    <span class="trail-icon">👨‍💼</span>
                    Atribuídas pelo Gestor
                </h3>
                <div id="assigned-trails" class="trail-list">
                    <!-- Trilhas serão carregadas dinamicamente -->
                    <div class="trail-loading">Carregando atribuições...</div>
                </div>
            </div>
        </div>
    </div>

    <!-- Seção de Ranking e Ações -->
    <div class="dashboard-section ranking-actions-section">
        
        <!-- RK: Ranking -->
        <div class="ranking-widget">
            <div class="widget-header">
                <h3 class="widget-title">
                    <span class="widget-icon">🏆</span>
                    Ranking PTDs
                </h3>
            </div>
            <div id="ptd-ranking" class="ranking-content">
                <!-- Ranking será carregado dinamicamente -->
                <div class="ranking-loading">Carregando ranking...</div>
            </div>
        </div>

        <!-- Ações Rápidas -->
        <div class="quick-actions">
            <div class="widget-header">
                <h3 class="widget-title">
                    <span class="widget-icon">⚡</span>
                    Ações Rápidas
                </h3>
            </div>
            <div class="actions-grid">
                <!-- FD: Feed -->
                <button class="action-card feed-action" onclick="window.location.href='/feed'">
                    <div class="action-icon">📰</div>
                    <div class="action-text">
                        <span class="action-title">Feed</span>
                        <span class="action-desc">Últimas novidades</span>
                    </div>
                </button>
                
                <!-- AT: Atendimento -->
                <button class="action-card support-action" onclick="window.location.href='/atendimento'">
                    <div class="action-icon">💬</div>
                    <div class="action-text">
                        <span class="action-title">Atendimento</span>
                        <span class="action-desc">Suporte técnico</span>
                    </div>
                </button>
            </div>
        </div>
    </div>

    <!-- CF: Cards Favoritados -->
    <div class="dashboard-section favorites-section">
        <div class="section-header">
            <h2 class="section-title">
                <span class="section-icon">❤️</span>
                Cards Favoritados
            </h2>
            <button id="refresh-favorites" class="refresh-btn" title="Atualizar favoritos">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
                </svg>
            </button>
        </div>
        
        <div id="favorite-cards" class="favorite-cards-container">
            <!-- Cards favoritos serão carregados dinamicamente -->
            <div class="favorites-loading">Carregando cards favoritos...</div>
        </div>
    </div>
</div>

<!-- Templates para elementos dinâmicos -->
<template id="trail-item-template">
    <div class="trail-item" data-trail-id="">
        <div class="trail-info">
            <div class="trail-name"></div>
            <div class="trail-progress">
                <div class="progress-bar">
                    <div class="progress-fill"></div>
                </div>
                <span class="progress-percentage"></span>
            </div>
        </div>
        <button class="trail-action-btn" onclick="goToTrail(this.dataset.trailId)">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
            </svg>
        </button>
    </div>
</template>

<template id="ranking-item-template">
    <div class="ranking-item" data-position="">
        <div class="ranking-position"></div>
        <div class="ranking-name"></div>
        <div class="ranking-score"></div>
    </div>
</template>

<template id="user-position-template">
    <div class="user-ranking-position">
        <div class="position-label">Sua posição:</div>
        <div class="position-info">
            <span class="position-number"></span>
            <span class="position-score"></span>
        </div>
    </div>
</template>

<template id="favorite-card-template">
    <div class="favorite-card" data-card-id="">
        <div class="card-content">
            <div class="card-header">
                <h4 class="card-title"></h4>
                <button class="unfavorite-btn" onclick="unfavoriteCard(this.dataset.cardId)" title="Desfavoritar">
                    <svg viewBox="0 0 24 24" fill="currentColor">
                        <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
                    </svg>
                </button>
            </div>
            <p class="card-description"></p>
            <div class="card-actions">
                <button class="download-btn" onclick="downloadCard(this.dataset.cardId)" title="Download">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                    </svg>
                </button>
            </div>
        </div>
    </div>
</template>

<script>
// Funções específicas para PTD
function goToTrail(trailId) {
    if (trailId) {
        window.location.href = `/trilha/${trailId}`;
    }
}

function unfavoriteCard(cardId) {
    if (confirm('Deseja realmente remover este card dos favoritos?')) {
        window.Dashboard.unfavoriteCard(cardId);
    }
}

function downloadCard(cardId) {
    window.Dashboard.downloadCard(cardId);
}

// Carregar dados específicos do PTD
document.addEventListener('DOMContentLoaded', function() {
    // Carregar trilhas ativas
    window.Dashboard.loadActiveTrails();
    
    // Carregar trilhas atribuídas
    window.Dashboard.loadAssignedTrails();
    
    // Carregar ranking
    window.Dashboard.loadPTDRanking();
    
    // Carregar cards favoritos
    window.Dashboard.loadFavoriteCards();
    
    // Event listeners para refresh
    document.getElementById('refresh-trails')?.addEventListener('click', function() {
        window.Dashboard.loadActiveTrails();
        window.Dashboard.loadAssignedTrails();
    });
    
    document.getElementById('refresh-favorites')?.addEventListener('click', function() {
        window.Dashboard.loadFavoriteCards();
    });
});
</script>